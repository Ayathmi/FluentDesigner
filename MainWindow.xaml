<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FluentDesigner.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentDesigner"
    xmlns:control="using:FluentDesigner.Control"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="FluentDesigner">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" x:Name="AppTitleBar" Height="32" VerticalAlignment="Center">
            <TextBlock Text="Fluent Designer"
                       VerticalAlignment="Center"
                       Margin="16, 0, 0, 0"
                       Style="{StaticResource CaptionTextBlockStyle}"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="*" MaxWidth="320"/>
                <ColumnDefinition Width="*" MaxWidth="480"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Row="0"
                    Grid.Column="0"
                    CornerRadius="4"
                    Margin="4"
                    Padding="4">
                <control:ViewportD3D12 x:Name="ViewportD3D12Control"/>
            </Border>

            <Border CornerRadius="4" Margin="4" Grid.Row="0" Grid.Column="1">
                <control:HierarchyView x:Name="HierarchyViewControl"/>
            </Border>

            <Border Grid.Row="0"
                    Grid.Column="2"
                    Grid.RowSpan="2"
                    CornerRadius="8"
                    Margin="4">
                <control:InspectorView x:Name="InspectorViewControl" 
                                       VerticalAlignment="Stretch" 
                                       HorizontalAlignment="Stretch"/>
            </Border>

            <Border Grid.Row="1"
                    Grid.Column="1"
                    CornerRadius="8"
                    Margin="4">
                <control:PlaybackPanel x:Name="PlaybackPanelControl"/>
            </Border>
            <Border Grid.Row="1"
                    Grid.Column="0"
                    CornerRadius="8"
                    Margin="4"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <control:TimelineControl x:Name="TimelineControlView"/>
            </Border>
        </Grid>
    </Grid>
</Window>
